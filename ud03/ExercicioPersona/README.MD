### COMENTARIOS
```Shell
Observamos que no existe ningún tipo de error sintáctico, puesto que al ejecutar el siguiente comando:

    $ mvn package

No da fallos de ejecución y se nos crea el ejecutable correspondiente en el subdirectorio '/target'
```